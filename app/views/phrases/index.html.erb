<h1>言葉の棚、 一覧ページ</h1>
  
  
<div >
  <% @phrase.each do |phrase| %>
      <p><%= phrase.phrase %></p>
      <p> <%= phrase.name %></p>
      <%= link_to "削除する", phrase_path(phrase.id), method: :delete %>
  <% end %>
</div>